// Seleccionar el elemento con la clase .fn-remove-me
const elementToRemove = document.querySelector(".fn-remove-me");

// Eliminar el elemento si existe
if (elementToRemove) {
    elementToRemove.remove();
}
